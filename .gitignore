# Node tooling for author-local math prerender (KaTeX). The vendored, pinned
# output lives in katex/ and IS committed; the install tree is not.
node_modules/

# Author-local figure demo output (regenerated on demand).
verification/figure_style_demo.png

# macOS Finder artifact
.DS_Store

# Expedition raw run dumps (regenerable search output, ~100MB+). The tracked
# record is expedition/records/*.jsonl; the instruments that produced them are
# committed. These are the raw output, not the record.
expedition/s5_run*/
expedition/s5_run*.log

# Python bytecode
__pycache__/
*.pyc
